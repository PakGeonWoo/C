#include <stdio.h>

int main(void) {


	// 배열 크기는 항상 상수로 선언(변수로 int형 지정해도 안돼)
	/*int subway_array[3];

	subway_array[0] = 30;
	subway_array[1] = 40;
	subway_array[2] = 50;

	for (int i = 0; i <= sizeof(subway_array); i++) {
		printf("%d\n", subway_array[i]);
	}*/

	/*int arr[] = { 1, 2 };*/

	return 0;

}
